# Digital-Image-Processing-Image-Segmentation
In this work we represent an image as a graph and segment its content with spectral clustering and n-cuts method (recursive and not). In the demo we try different images and different hyperparameters to showcase the results and the general behavior.
